# Transformation d'un fichier xls ou csv en xml

"""from pathlib import PurePath

def table_to_xml(file_name):
    file_extension = PurePath(file_name).suffix

    xml_file_name = PurePath(file_name).with_suffix('.xml')

    return xml_file_name"""


"""def inverse_chaine(chaine):
    if isinstance(chaine, str):

        return chaine[::-1]
    elif isinstance(chaine, list):

        result = []
        for i in chaine:
            if isinstance(i, str):
                return result.append(i[::-1])
            else:
                result.append(None)

        return result
    else:
        return None
"""